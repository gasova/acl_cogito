<div>
  <%= paginate comments %>
</div>

<ul id="comments">
  <% unless comments.empty? %>
    <%= render :partial => 'acl_cogito/comments/comment', :collection => comments %>
  <% else %>
    <li id="no_comments"><%= t('acl_cogito.messages.no_comments_found') %></li>
  <% end %>
</ul>

<div>
  <%= paginate comments %>
</div>